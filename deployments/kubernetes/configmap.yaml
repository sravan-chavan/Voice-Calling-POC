apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-calling-config
  labels:
    app: voice-calling
data:
  # Non-sensitive configuration only
  PLIVO_FROM_NUMBER: "+14151234567"
  PLIVO_ANSWER_URL: "https://s3.amazonaws.com/static.plivo.com/answer.xml"
  PLIVO_ANSWER_METHOD: "GET"
  HTTP_ADDR: ":8080"
